meta {
  name: Info
  type: http
  seq: 2
}

get {
  url: {{baseUrl}}/actuator/info
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("info endpoint responds successfully", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
  });
}
