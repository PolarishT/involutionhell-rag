meta {
  name: RAG Logger
  type: http
  seq: 4
}

get {
  url: {{baseUrl}}/actuator/loggers/com.involutionhell.backend.rag
  body: none
  auth: none
}

headers {
  Accept: application/json
}

tests {
  test("logger endpoint is readable", function() {
    expect(res.status).to.equal(200);
    expect(res.body).to.be.an("object");
  });
}
